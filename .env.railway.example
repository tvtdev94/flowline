# ===========================================
# FLOWLINE - Railway Environment Variables
# ===========================================
# Copy these variables to Railway Dashboard
# Project > Service > Variables tab

# ===========================================
# BACKEND SERVICE VARIABLES
# ===========================================

# -------------------------------------------
# Database Connection
# -------------------------------------------
# Railway auto-generates these from PostgreSQL service
# Use Railway's variable reference syntax:
ConnectionStrings__DefaultConnection=Host=${{Postgres.PGHOST}};Port=${{Postgres.PGPORT}};Database=${{Postgres.PGDATABASE}};Username=${{Postgres.PGUSER}};Password=${{Postgres.PGPASSWORD}}

# -------------------------------------------
# Google OAuth Configuration
# -------------------------------------------
# Get from: https://console.cloud.google.com/
Google__ClientId=your-google-client-id.apps.googleusercontent.com
Google__ClientSecret=your-google-client-secret

# -------------------------------------------
# JWT Configuration
# -------------------------------------------
# Generate with: openssl rand -base64 32
JWT_SECRET=generate-a-secure-random-string-min-32-characters-long
Jwt__Secret=${{JWT_SECRET}}
Jwt__Issuer=flowline-api
Jwt__Audience=flowline-app
Jwt__ExpiryMinutes=60

# -------------------------------------------
# ASP.NET Core Configuration
# -------------------------------------------
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:5000
PORT=5000

# -------------------------------------------
# Development Mode
# -------------------------------------------
# Set to false to enable Google Auth (recommended for production)
Development__EnableGoogleAuth=true

# Optional: Mock user for testing (only if EnableGoogleAuth=false)
# Development__MockUser__Id=00000000-0000-0000-0000-000000000001
# Development__MockUser__Email=dev@flowline.railway.app
# Development__MockUser__Name=Development User
# Development__MockUser__GoogleId=dev-google-id

# ===========================================
# FRONTEND SERVICE VARIABLES
# ===========================================

# -------------------------------------------
# Backend API URL
# -------------------------------------------
# Replace with your Railway backend URL after deployment
VITE_API_URL=https://your-backend-name.up.railway.app

# -------------------------------------------
# Google OAuth (Frontend)
# -------------------------------------------
# Must match backend configuration
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ===========================================
# POSTGRESQL SERVICE
# ===========================================
# Railway auto-generates these - no need to set manually:
# - DATABASE_URL
# - PGHOST
# - PGPORT
# - PGDATABASE
# - PGUSER
# - PGPASSWORD

# ===========================================
# NOTES
# ===========================================
# 1. Don't commit this file with real values
# 2. Use Railway's UI to add variables
# 3. Use ${{SERVICE.VARIABLE}} syntax to reference other services
# 4. Railway automatically exposes PORT for networking
# 5. Database credentials are auto-injected by Railway
